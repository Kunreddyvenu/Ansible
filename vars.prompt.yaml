- name: vars prompt
  hosts: localhost
  vars_prompt:
  - name: username
    prompt: please enter username
    private: false
  - name: password
    prompt: please enter password
    private: true
  tasks:
  - name: giving the prompt
    ansible.builtin.debug:
      msg: "hi, username: {{username}}. password is: {{password}}"