- name: variables
  hosts: localhost
  vars: #variable declaration
    cource: "kuberneties"
    trainer: "venu"
    duration: "150hrs"
  tasks:
  - name: print infromation
    ansible.builtin.debug:
      msg: "hi, we are learnig {{cource}} from {{trainer}} and the time {{duration}}."


- name: variables
  hosts: [web]
  vars: #Declare vars
    cource1: "ansible"
    trainer1: "siva"
    duration1: "120hrs"
  tasks:
  - name: print the infromation
    ansible.builtin.debug:
      msg: "hi, i am learnign {{cource1}} from {{trainier1}} and timing is {{duration1}}."