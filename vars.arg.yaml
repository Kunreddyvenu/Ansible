- name: vars from argument
  hosts: localhost
  tasks:
  - name: get from args
    ansible.builtin.debug:
      msg: "I am learnig {{name}} and duration {{time}}"

#we can pause the arguments while executing script