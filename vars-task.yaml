- name: vars over write
  hosts:  localhost
  vars: #Declare vars
    cource: "ansible"
    trainer: "siva"
    duration: "120hrs"
  tasks:
  - name: print information
    vars:
      cource: "terraform"
    ansible.builtin.debug:
      msg: "hi i am learning cource {{cource}} from trainer {{siva}} duration{{duration}}."

- name: Print infromation
  ansible.builtin.debug:
    msg: "hi we are learning {{cource}} from trainer {{trainier}} duration{{duration}}."
